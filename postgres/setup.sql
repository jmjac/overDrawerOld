CREATE TABLE blocks (
    id serial primary key,
);

CREATE TABLE transactions (
    id serial primary key,
);

CREATE TABLE addresses (
    id serial primary key,

);

CREATE TABLE identities (
    id serial primary key,
);
